var generate = function(numRows) {
    let output = [];
    if (numRows == 0) output = [];
    if (numRows == 1) output = [[1]];
    for(let i=1; numRows.length>=i;i++){
        let result = [];
        
        output.push(result);
    }
};